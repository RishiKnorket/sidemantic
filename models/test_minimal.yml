models:
  - name: test
    table: test_table
    primary_key: id
    
    dimensions:
      - name: id
        type: categorical
        sql: id
    
    metrics:
      - name: count
        type: number
        agg: count
        sql: "*"
